apiVersion: v1
kind: Service
metadata:
  name: syon-api
  labels:
    app: syon
spec:
  type: ClusterIP
  selector:
    app: syon
    component: api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP